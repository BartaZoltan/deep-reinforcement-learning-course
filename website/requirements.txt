# Build requirements for the course website (Jupyter Book)
# Install with: pip install -r website/requirements.txt

# Pin to the Python-only Jupyter Book 1.x series (Jupyter Book 2.x requires Node.js)
jupyter-book>=1.0.0,<2
